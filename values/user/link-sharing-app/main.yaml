# Overrides for the 'link-sharing-app' from the 'main' branch

image:
  tag: "main"
  # Updated by CI on each main push to create an immutable deployment reference.
  digest: "sha256:b4398676525a890b0d8f4f6d8128606f7482ae6c905281255d61fffa8e70c12b"
  pullPolicy: IfNotPresent

# Private GHCR pull secret
imagePullSecretName: "ghcr-credentials"

ingress:
  host: link-sharing-app-main.vhovet.com

networkPolicy:
  enabled: true

resources:
  requests:
    cpu: 50m
    memory: 128Mi

databaseResources:
  requests:
    cpu: 50m
    memory: 256Mi
