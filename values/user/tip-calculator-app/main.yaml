# Overrides for the 'tip-calculator-app' from the 'main' branch

image:
  tag: "latest"
  pullPolicy: Always

ingress:
  host: tip-calculator-dev-main.vhovet.com

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi