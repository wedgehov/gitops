# Overrides for the 'fm-todo-app' from the 'main' branch

image:
  tag: "main"
  # Updated by CI on each main push to create an immutable deployment reference.
  digest: "sha256:8e5782806c3d2bc54614c0a2a3bd77287d4c683d9ae0e63d7ce4bf8580b824d4"
  pullPolicy: IfNotPresent

ingress:
  host: fm-todo-app-main.vhovet.com

networkPolicy:
  enabled: true

resources:
  requests:
    cpu: 50m
    memory: 128Mi

databaseResources:
  requests:
    cpu: 50m
    memory: 256Mi
