# Overrides for the 'fm-todo-app' from the 'main' branch

image:
  tag: "main"
  # Updated by CI on each main push to create an immutable deployment reference.
  digest: "sha256:c33ef0913ad3453604e50948e8a8da12950f2e2b3199264212b4bc94be6d9011"
  pullPolicy: IfNotPresent

ingress:
  host: fm-todo-app-main.vhovet.com

networkPolicy:
  enabled: true

resources:
  requests:
    cpu: 10m
    memory: 128Mi

databaseResources:
  requests:
    cpu: 10m
    memory: 256Mi
