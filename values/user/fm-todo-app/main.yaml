# Overrides for the 'fm-todo-app' from the 'main' branch

image:
  tag: "main"
  # Updated by CI on each main push to create an immutable deployment reference.
  digest: "sha256:a9bca2f6ef78436cb0e2266ad2bdd21aefc51195f083ab2ca9e6a5c6926f409a"
  pullPolicy: IfNotPresent

ingress:
  host: fm-todo-app-main.vhovet.com

networkPolicy:
  enabled: true

resources:
  requests:
    cpu: 10m
    memory: 128Mi

databaseResources:
  requests:
    cpu: 10m
    memory: 256Mi
