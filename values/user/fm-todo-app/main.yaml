# Overrides for the 'fm-todo-app' from the 'main' branch

image:
  tag: "main"
  # Updated by CI on each main push to create an immutable deployment reference.
  digest: "sha256:a248cd623b918fc529bb2f92712737d7366094803a5bc30beed60b25bc616940"
  pullPolicy: IfNotPresent

ingress:
  host: fm-todo-app-main.vhovet.com

networkPolicy:
  enabled: true

resources:
  requests:
    cpu: 10m
    memory: 128Mi

databaseResources:
  requests:
    cpu: 10m
    memory: 256Mi
